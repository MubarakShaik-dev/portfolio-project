import React from 'react';
import { SpinnerWrapper, StyledSpinner } from './styles';

const Spinner = () => {
  return (
    <SpinnerWrapper>
      <StyledSpinner />
    </SpinnerWrapper>
  );
};

export default Spinner;
